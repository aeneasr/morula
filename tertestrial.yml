name: 'Golang: Cucumber (godog)'
actions:

  - match:
    command: 'go install ; godog'

  - match:
      filename: '\.feature$'
    command: 'go install ; godog {{filename}}'

  - match:
      filename: '\.feature$'
      line: '\d+'
    command: 'go install ; godog {{filename}}:{{line}}'
